/**
 * Pre-Flight Day 7 Tasks: Tooling Checklist
 */

// 1. Initialize a git repo (run in terminal)
// command: git init

// 2. Create a .gitignore file
// command: echo "node_modules/\n.env" > .gitignore

// 3. Practice Debugging:
// Set a breakpoint on the line below and run with F5 in VS Code.
function mystery(a, b) {
    let result = a * b; // Check 'result' in the debugger
    return result + 10;
}

mystery(5, 5);

console.log("Pre-Flight Phase Complete.");